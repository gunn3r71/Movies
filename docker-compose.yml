version: '3.9'

services:
  db:
    image: postgres:latest
    restart: always
    environment:
      - POSTGRES_USER=movies
      - POSTGRES_PASSWORD=chang3m3
      - POSTGRES_DB=movies
    ports:
      - '5432:5432'